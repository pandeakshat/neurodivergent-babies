---
// src/components/TestimonialCard.astro
interface Props {
  quote: string;
  author: string;
}

const { quote, author } = Astro.props;
---

<blockquote class="p-7 rounded-lg shadow-md border border-border bg-card text-left">
  <p class="text-lg mb-4 text-foreground">"{quote}"</p>
  <footer class="font-semibold text-primary">— {author}</footer>
</blockquote>